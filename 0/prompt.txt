You are in a git worktree of Nxtsoft/blastline on branch pr-brief (cwd). Do exactly these steps, nothing else. 1) Read .agents/scratch/changelog-0.13.0.md and create CHANGELOG.md at the repository root with that content verbatim (byte for byte). 2) In package.json change the line "version": "0.12.1" to "version": "0.13.0"; change nothing else in that file. 3) Run: bun run build && bunx vitest run   and confirm it reports 17 test files passed. 4) Commit only those two files: git add CHANGELOG.md package.json && git commit CHANGELOG.md package.json -m 'docs(changelog): 0.13.0, the PR brief' -m 'CHANGELOG.md is new; package.json goes to 0.13.0 for the release that ships blastline brief, the blastline_brief MCP tool, the check run and the Action changes.' -m 'Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>'   Never pass --no-verify. Do not push. Do not touch any other file. 5) Report the output of: git log -1 --format=%B   and of: git for-each-ref refs/entire   and of: git status --short